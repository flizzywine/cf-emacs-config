# -*- mode: snippet -*-
# name: testSuite
# key: test
# --
	vector<vector<int> > arr = {$0};
	int ar[20] = {};
	for(int i=0; i<arr.size(); i++) {
		for(int j = 0; j<arr[i].size(); j++) {
			ar[j] = arr[i][j];
		}
		int n = arr[i].size();
		

	}